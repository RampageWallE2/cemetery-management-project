import { Routes } from '@angular/router';
import { CemeteryMap } from './features/cemetery-map/cemetery-map';

export const routes: Routes = [
    {
        path : '',
        component: CemeteryMap
    }
];
